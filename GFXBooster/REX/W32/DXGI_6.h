#pragma once

#ifndef WIN32_LEAN_AND_MEAN
#define WIN32_LEAN_AND_MEAN
#endif
#include <Windows.h>

#include <dxgi1_6.h>

// If the project ever references DXGI 1.6 interfaces via REX::W32,
// add aliases here as needed.
namespace REX
{
    namespace W32
    {
        // (currently empty by design)
    }
}
